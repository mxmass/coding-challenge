import React, { useEffect } from 'react';
// import { useDispatch, useSelector } from "react-redux";
// import { fetchList } from "../reducers/product";

const List = () => {
//   const list = useSelector(state => state.productReducer.filtered);
//   const dispatch = useDispatch(); 

//   useEffect(() => {
//       setTimeout(() => dispatch(fetchList()), 2000)
//   }, [dispatch])

  return (
    <>
        List
    </>
  );
}

export default List;